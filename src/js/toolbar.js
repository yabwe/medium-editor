/*global  */

var Toolbar;

(function (window, document) {
    'use strict';

    Toolbar = function Toolbar(instance) {
        this.base = instance;
    };

    Toolbar.prototype = {

    };
}(window, document));